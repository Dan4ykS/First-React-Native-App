import React from 'react';
import { CustomText } from '../ui/CustomElements';
import { styles } from '../../styles';
const CartScreen = () => {
  return <CustomText style={styles.textHeader}>Корзина!!!!</CustomText>;
};

export default CartScreen;
